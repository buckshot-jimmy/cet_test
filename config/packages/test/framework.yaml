framework:
    profiler:
        enabled: true
        collect: true
    test: true
    validation:
        enable_annotations: true
    session:
        enabled: true
        storage_factory_id: session.storage.factory.mock_file